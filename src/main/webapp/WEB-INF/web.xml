<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://java.sun.com/xml/ns/javaee"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd"
         id="WebApp_ID" version="2.5">

  <!-- 更多配置元素参考： https://docs.oracle.com/cd/E17904_01/web.1111/e13712/web_xml.htm#WBAPP537 -->

  <display-name>Archetype Created Web Application</display-name>

  <!-- 配置error-page -->
  <!-- 用于在错误发生时返回错误页面（包括错误码和错误信息） -->
  <!--<error-page>-->
  <!--<error-code>404</error-code>-->
  <!--<location>errorPage.jsp</location>-->
  <!--</error-page>-->

  <!-- 配置监听器，可以通过多个<listener>配置多个监听器 -->
  <listener>
    <!-- Web服务器接收的每次请求都会通知该监听器，RequestContextListener用于支持Bean的另外3个作用域request、session、globalSession -->
    <listener-class>org.springframework.web.context.request.RequestContextListener</listener-class>
  </listener>
  <listener>
    <!-- Web容器启动时自动装配ApplicationContext配置信息，如果在web.xml中不写任何参数配置信息，默认的路径是/WEB-INF/applicationContext.xml，
        在WEB-INF目录下创建的xml文件的名称必须是applicationContext.xml；applicationContext.xml中配置的bean会在web容器启动时加载 -->
    <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
  </listener>
  <!-- 通过<context-param>修改applicationContext.xml存放路径，IDEA的classpath即为target/classes -->
  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>
      classpath:applicationContext.xml
    </param-value>
  </context-param>

  <!-- 配置过滤器, encoding设置为UTF-8可支持显示中文 -->
  <filter>
    <filter-name>characterEncodingFilter</filter-name>
    <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
    <init-param>
      <param-name>forceEncoding</param-name>
      <param-value>true</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>characterEncodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- servlet -->
  <!-- 通过别名island_servlet将Spring DispatcherServlet与 /user/* 连接起来 -->
  <servlet>
    <servlet-name>island</servlet-name>
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    <!--<init-param>-->
      <!--<param-name>contextConfigLocation</param-name>-->
      <!--其中<param-value>**.xml</param-value> 这里可以使用多种写法-->
      <!--1、不写,使用默认值:/WEB-INF/<servlet-name>-servlet.xml-->
      <!--2、<param-value>/WEB-INF/classes/dispatcher-servlet.xml</param-value>-->
      <!--3、<param-value>classpath*:dispatcher-servlet.xml</param-value>-->
      <!--4、多个值用逗号分隔-->
      <!--<param-value>classpath:dispatcher-servlet.xml</param-value>-->
    <!--</init-param>-->
    <load-on-startup>1</load-on-startup> <!-- 启动时加载 -->
  </servlet>
  <servlet-mapping>
    <servlet-name>island</servlet-name>
    <url-pattern>*.do</url-pattern>
    <!--<url-pattern>/*</url-pattern>-->
    <!--<url-pattern>/module/*</url-pattern>-->
  </servlet-mapping>

</web-app>
